/**
 * Blur region drawing tool.
 * Allows users to draw rectangles over areas to blur in the editor.
 * Will be implemented in Phase 4.
 */

function BlurRegionTool(): JSX.Element {
  return (
    <div className="absolute inset-0 pointer-events-none">
      {/* Phase 4: interactive blur region drawing overlay */}
    </div>
  )
}

export default BlurRegionTool
